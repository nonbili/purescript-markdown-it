{
  "license": "BSD-3-Clause",
  "scripts": {
    "build": "pulp build -I test -- --strict --stash +RTS -N2 -RTS",
    "postinstall": "bower install",
    "test": "pulp build -I test && jest"
  },
  "devDependencies": {
    "jest": "^23.6.0",
    "markdown-it-imsize": "^2.0.1"
  },
  "dependencies": {
    "markdown-it": "^9.0.1"
  }
}
